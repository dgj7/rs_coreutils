name: rwc

on:
  push:
    paths:
    - rwc/**
  pull_request:
    paths:
    - rwc/**

env:
  CARGO_TERM_COLOR: always

jobs:
  test-build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Test
      run: cargo test --verbose
      working-directory: ./rwc
    - name: Build
      run: cargo build --examples --verbose
      working-directory: ./rwc
